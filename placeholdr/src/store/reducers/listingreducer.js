const initState = {
    listings: [
        {id: '1',address: '420 University Boulevard', distance: 8}
    ]
}

const listingReducer = (state = initState,action) => {
    return state;
}

export default listingReducer;